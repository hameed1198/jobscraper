import { Bookmark, Building2, ExternalLink, MapPin } from "lucide-react";
import type { Job } from "../types";
export function JobCard({ job, saved, onSave, onView }: { job: Job; saved: boolean; onSave: () => void; onView: () => void }) {
 return <article className="job-card"><div className="company-mark">{job.company.slice(0, 1)}</div><div className="job-main"><div className="card-top"><div><p className="eyebrow">{job.source} · {job.posted}</p><h3>{job.title}</h3><p className="company"><Building2 size={15}/>{job.company}</p></div><div className={`score ${job.match >= 85 ? "great" : ""}`}><strong>{job.match}%</strong><span>match</span></div></div><div className="details"><span><MapPin size={15}/>{job.location}</span><span>{job.mode}</span><span>{job.salary}</span></div><p className="description">{job.description}</p><div className="card-footer"><div className="tags">{job.tags.map(tag => <span key={tag}>{tag}</span>)}</div><div className="actions"><button className={saved ? "saved" : ""} onClick={onSave} aria-label="Save job"><Bookmark size={18} fill={saved ? "currentColor" : "none"}/></button><button className="view-role" onClick={onView}>View role <ExternalLink size={15}/></button></div></div></div></article>;
}
